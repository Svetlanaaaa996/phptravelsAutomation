package login;

import java.util.UUID;

public class RegisterTests {
	// Generate a random email address
    String randomText = UUID.randomUUID().toString();
    String email = "test+" + randomText + "@gmail.com";
	
}
